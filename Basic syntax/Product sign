#include <iostream>
#include<cmath>
#include<string>
#include<algorithm>
using namespace std;

int main()
{
    int x, y, z;
    cin >> x >> y >> z;

    if (x * y * z >= 0) cout << "+" << endl;
    else cout << "-" << endl;

    return 0;
}
